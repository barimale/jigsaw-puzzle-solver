namespace Genetic.Algorithm.Tangram.Solver.Domain.Generators
{
    // WIP
    public class BlockRotationsMatrixGenerator
    {
        public static void GenerateArray(double singleStepInDegrees, double rotationScaleInDegrees = 360)
        {
            //double stepsAmount = rotationScaleInDegrees / singleStepInDegrees;

            //foreach(var (item, index) in  Enumerable.Range(0, (int)stepsAmount))
            //{

            //}
        }
    }
}
